import css from "@/styles/landing.module.css";
export default function MyProjects() {
  return (
    <div
      className={`${css.root} flex flex-grow flex-col items-center justify-center`}
    >
      <h3>Mis Proyectos</h3>
    </div>
  );
}
